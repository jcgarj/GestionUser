{% extends '::base.html.twig' %}

{% block body -%}
    <h1>dele_muni</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Delemuni</th>
                <td>{{ entity.delemuni }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('dele_muni') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('dele_muni_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
