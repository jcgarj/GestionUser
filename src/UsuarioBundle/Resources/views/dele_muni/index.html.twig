{% extends '::base.html.twig' %}

{% block body -%}
    <h1>dele_muni list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Delemuni</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('dele_muni_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.delemuni }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('dele_muni_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('dele_muni_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('dele_muni_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
